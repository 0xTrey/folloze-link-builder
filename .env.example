# Postgres connection string used for 24 hour session storage.
# Use a dedicated application role and configure this in Vercel or .env.local.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
